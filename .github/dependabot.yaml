version: 2

updates:
  - package-ecosystem: github-actions
    directories:
      - /
      - /build-expo-app
      - /deploy-expo-app
      - /setup-eas
      - /setup-java
      - /setup-turborepo
      - /update-expo-app
    schedule:
      interval: weekly
    commit-message:
      prefix: chore
