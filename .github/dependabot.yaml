version: 2

updates:
  - package-ecosystem: github-actions
    directories:
      - /
      - /build-expo-app
      - /setup-turborepo
    schedule:
      interval: weekly
    commit-message:
      prefix: chore
